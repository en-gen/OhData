# OhData

Convention-based OData 4.0 server and typed client for ASP.NET Core. Define a profile class, assign handler delegates, and get a spec-faithful OData API — no controllers required.

> This is a **thin proof-of-concept** DocFX site for issue #208, scaffolded to confirm the recommended toolchain builds locally. It contains a landing page plus three guides **copied** from `docs/`. It is not deployed and not published.

## Explore

- [Architecture](architecture.md) — the core flow and design decisions
- [Query options](query-options.md) — `$filter` / `$orderby` / `$select` / `$expand` / `$count` / `$search`
- [Authorization](authorization.md) — per-profile auth, roles, and `RequireResource`

See `docs-site-proposal/` for the full SSG recommendation, information architecture, and publish workflow.
