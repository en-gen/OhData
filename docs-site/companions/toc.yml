# NESTED toc for the "Companions" product tab (issue #208) - renders in the modern LEFT SIDEBAR.
#
# Owns the companion-integration guides (OpenAPI / NSwag). Pages output into this
# companions/ folder (docfx.json `dest`), so companions/toc.html is their owning toc and the
# modern template renders it on the left. Single-sourced from the repo's docs/ folder; one
# level deep, so hrefs are ../../docs/*.md.
#
# NOTE: Swashbuckle has no standalone page - its setup is covered inside openapi.md and
# versioning.md - so there is intentionally no Swashbuckle node here.

- name: OpenAPI (built-in AddOpenApi)
  href: ../../docs/openapi.md

- name: NSwag
  href: ../../docs/nswag.md
